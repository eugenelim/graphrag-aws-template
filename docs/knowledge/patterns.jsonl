{"id": "K-0001", "kind": "pattern", "scope": "packages/graphrag/**", "title": "Resolution is upsert-by-normalized-ID, not a merge pass", "body": "Cross-source entity resolution falls out of giving each mention a normalized ID (normalize.py) and letting Graph.upsert_node union sources/props on collision. There is no separate matching step. When adding an entity kind or a new source, the only resolution work is choosing the ID function — get that right and merges are automatic and narratable. The alias table (aliases.yaml) is the sole non-mechanical step.", "source": "PR (graph-ingestion-resolution)"}
{"id": "K-0002", "kind": "pattern", "scope": "packages/graphrag/store/**", "title": "Traverse over neighbors(), not openCypher, to keep backends identical", "body": "Multi-hop traversal runs in query.py over GraphStore.neighbors(), so MemoryGraphStore and NeptuneGraphStore produce a byte-identical trace and the whole graph path is testable without a live cluster. Pushing traversal into openCypher would diverge the backends and break the offline demo/test story — it is an explicit Boundaries rail, deferred to slice 3.", "source": "ADR-0001"}
{"id": "K-0003", "kind": "gotcha", "scope": "packages/graphrag/store/neptune.py", "title": "openCypher can't parameterize relationship types — use one REL type + a kind param", "body": "Relationship types are not parameterizable in openCypher, so the naive edge query interpolates the kind into the string (an injection vector). Model every edge as a single REL type carrying a `kind` property; then MATCH (a)-[r:REL {kind: $kind}]->(b) keeps kind a bound parameter. Same trick keeps node ids parameterized via a single Entity label.", "source": "PR (graph-ingestion-resolution)"}
{"id": "K-0004", "kind": "gotcha", "scope": "packages/graphrag/parse.py", "title": "Always yaml.safe_load — the corpus is untrusted input parsed under an IAM role", "body": "The Fargate task parses external repo YAML while holding an S3+Neptune+STS task role, so a !!python/object tag under yaml.load would be RCE in the VPC (CWE-502). Use yaml.safe_load everywhere; the ruff S ruleset (S506) catches a regression. A negative fixture (tests/fixtures/unsafe.yaml) asserts the tag parses inert.", "source": "security-review (spec stage)"}
{"id": "K-0005", "kind": "gotcha", "scope": "apps/infra/**", "title": "CDK synth tests: least-privilege check must exclude ecr:GetAuthorizationToken", "body": "Asserting 'no IAM statement has Resource \"*\"' fails on a legitimate AWS requirement: ecr:GetAuthorizationToken on the execution role must be \"*\". Scope the least-privilege assertion to the task role's app actions (neptune-db:connect, s3 read) instead. Also: set JSII_SILENCE_WARNING_UNTESTED_NODE_VERSION so the node-version warning doesn't clutter output; Template.from_stack synthesizes in-process with no cdk CLI or AWS account.", "source": "PR (graph-ingestion-resolution)"}
